# RAG Search Engine

A command-line project for exploring information retrieval techniques.

## Usage

Run the keyword search CLI from the repository root:

```bash
uv run cli/keyword_search_cli.py search "grizzly"
```
